/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  i18n: {
    locales: ['it'],
    defaultLocale: 'it',
    localeDetection: false,
  },
  // Aggiungeremo configurazioni specifiche qui (es. i18n, output, etc.)
};

export default nextConfig; 